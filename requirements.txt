# Core ML Dependencies
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# ONNX and Runtime
onnx>=1.15.0
onnxruntime>=1.17.0

# Computer Vision
opencv-python>=4.9.0.80
Pillow>=10.0.0

# Numerical Computing
numpy>=1.24.0
scipy>=1.15.0

# Vision Transformers and Models
timm==0.4.12  # DDN dependency: Vision Transformer library

# Image Processing and Visualization
imageio>=2.31.0  # DDN dependency: Image I/O
matplotlib>=3.7.0  # DDN dependency: Visualization and plotting

# ConceptAttention for Phase 2 Saliency Integration
# Option 1: Full ConceptAttention package (recommended)
# -e git+https://github.com/helblazer811/ConceptAttention.git#egg=concept_attention

# Option 2: Minimal dependencies (fallback when full package not available)
diffusers>=0.25.0  # Required for Flux pipeline (both full and minimal)
transformers>=4.36.0  # Required for text processing (both full and minimal)

# CLI and Utilities
click>=8.0.0
typer>=0.9.0
rich>=13.0.0
tqdm>=4.65.0

# Development and Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Logging and Monitoring
loguru>=0.7.0

# Data Handling
PyYAML>=6.0

# DDN-specific Dependencies
# Note: These are required for DDN (Doubly Decoupled Network) integration
# timm: Pre-trained vision models and CAFormer backbone
# imageio: Enhanced image loading for DDN preprocessing  
# scipy: Scientific computing for DDN's Gaussian distribution operations
# matplotlib: Visualization for debugging and evaluation

# Optional: For model optimization
# onnxoptimizer>=0.3.13 